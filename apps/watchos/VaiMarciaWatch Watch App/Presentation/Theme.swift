import SwiftUI

/// Cores provisórias da marca TogPlay — coral/laranja + navy. Substituir pelos valores finais
/// do brand kit assim que o design entregar a paleta oficial.
enum TogPlayColor {
    static let coral = Color(red: 1.0, green: 0.42, blue: 0.29) // #FF6B4A
    static let navy = Color(red: 0.09, green: 0.13, blue: 0.23) // #17213A
    static let surface = Color(red: 0.12, green: 0.17, blue: 0.29) // #1F2B4A
}
