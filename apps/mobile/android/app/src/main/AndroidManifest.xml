<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth: modelo de permissão em tempo de execução do Android 12+
         (API 31+). BLUETOOTH_CONNECT/SCAN/ADVERTISE são necessárias para
         enumerar caixas de som A2DP pareadas e (futuramente) anunciar o
         serviço companion para o smartwatch. `usesPermissionFlags="neverForLocation"`
         evita o agrupamento com permissão de localização que o Android
         normalmente força em scans BLE, já que este app nunca precisa da
         localização do dispositivo. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Permissões legadas de Bluetooth para dispositivos com API < 31. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Mantém a rota A2DP + a sessão do Data Layer do Wear OS ativas
         enquanto o app está em segundo plano, conforme ARCHITECTURE.md §4
         ("conexão mantida aquecida"). Declarada como tipo `mediaPlayback`
         — o start/stop real do foreground service é um TODO nativo, veja
         android/app/src/main/kotlin/.../PlaybackForegroundService.kt. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:label="Vai Márcia"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Suporte a deep link para lojatogplay.com.br (loja) e
                 futuros links com esquema vaimarcia:// vindos de
                 notificações. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.lojatogplay.com.br" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- TODO(platform-channel): registrar o PlaybackForegroundService
             aqui assim que BluetoothTransportPlugin.kt implementar o
             comportamento "manter a rota A2DP aquecida" descrito em
             ARCHITECTURE.md §4. -->
    </application>
</manifest>
